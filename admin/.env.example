# URL бэкенда (без /glasses в конце)
VITE_BASE_URL=http://localhost:3000

# URL клиента (8th Wall AR) — используется как src iframe в превью
# Локально: http://localhost:8080
# Продакшн: https://client.your-domain.com (обязательно HTTPS)
VITE_CLIENT_URL=http://localhost:8080
